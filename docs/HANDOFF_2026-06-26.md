# CowTalk 세션 핸드오프 — 2026-06-26 (인수위 브리핑 1시간 전)

> 다음 세션은 이 문서부터 읽고 이어가세요. 브리핑 직후 보완 작업 재개.

## 이번 세션에 머지된 것 (main 기준, 최신순)

| PR | 제목 | 핵심 |
|---|---|---|
| #105 | 경기 61 vs 52 불일치 — 시·군명 권위 판별로 시도 통일 | 시도 판별 단일화 |
| #104 | 공공데이터 융합 가이드 (이력제·등급·경락·혈통) | 팅커벨 프롬프트 강화 |
| #103 | smaXtec↔CowTalk 착유우 불일치 + 농장 펄스 선제 주입 | 우군 분류 enum 통일 |
| #102 | 선제요약 확장(번식·건강) + 응답 후 정정 학습 루프 | (이전 세션) |

현재 작업 브랜치: `claude/fervent-goldberg-m9d9c2` (= origin/main 동기화 상태, 클린)

## ⚠️ 시연 중 반드시 확인할 것 (배포본에서 1회씩)

1. **방역 대시보드 경기 카운트 = 61** (또는 smaXtec 실측치)와 일치하는지.
   - #105가 배포 반영돼야 함. 드릴다운 목장 수 = 집계 수 동일해야 함.
   - 만약 여전히 52~60 사이면 → 일부 농장명에 시·군이 없어 좌표 fallback 중.
     그 농장 목록을 확보하면 매핑/좌표 보강 가능(아래 "다음 작업" 참조).
2. **해돋이목장(포천) 착유우 = 66두** (smaXtec과 일치, #103). 팅커벨이 "착유 0"
   같은 답을 다시 내면 회귀.
3. **팅커벨 "경기도 목장 현황" / "충북 현황"** → 깡통응답·타임아웃 없이 해당
   지역만 정확히 답하는지 (스코프 + 선제요약 주입).
4. **공공데이터 융합(#104)**: "이 소 출하하면 등급 어때?" 류 질문에서
   query_animal→traceId→query_grade 체이닝이 도는지. ⚠️ 도축 전 개체는 등급
   데이터 없음이 정상(환각 금지 가드 작동 확인). EKAPE 실연결 스모크 미검증.

## 핵심 아키텍처 메모 (디버깅 시 진입점)

### 시도 판별 — 단일 권위 함수 (#105)
- `packages/server/src/services/epidemiology/province-mapper.ts`
  - `cityToProvince(address)` — farm.address(=smaXtec org명 괄호 안 시·군)로 시도 판별. 권위.
  - `resolveFarmProvince({regionProvince, address, lat, lng})` — `region > 주소 시·군 > 좌표` 우선순위. **모든 시도 집계가 이 함수만 사용.**
  - 동일지명(광주·고성)은 매핑 제외 → 좌표 fallback(새 오류 방지).
- 소비자 4곳 전부 경유(발산 방지): `national-situation.service.ts`(방역 대시보드),
  `unified-dashboard.routes.ts`(농장 드롭다운), `herd-service.ts`(두수), `alert-aggregator.ts`(알림).

### 알려진 잔존 이슈 (근본 원인, 미해결)
- `sync.service.ts:119` — smaXtec 농장 좌표가 `(36.5, 127.5)` **하드코딩**.
  city가 주소에 있으면 무의미해지지만, **농장명에 시·군이 없는 농장은 여전히 충북으로 오분류**됨. 진짜 해결은 실제 지오코딩(주소→좌표) 또는 regionId를 올바른 시도 region으로 재배정.
- `sync.service.ts:49-51` — 모든 농장이 province='전국' placeholder region 공유.
  근본 해결: 시·군 기반으로 올바른 region에 매핑(백필 필요, DB 접근 요).

### 공공데이터 저장 실태 (융합 전략 근거, #104)
- **DB 상주(싸게 융합 가능)**: `semenCatalog`(씨수소 유전능력), `farmSemenInventory`, `genomicData`. 씨수소는 `recommend_insemination_window`로 이미 융합됨.
- **외부 EKAPE only(느림 → 온디맨드 도구만, 선제호출 금지)**: 이력제·등급·경락·혈통·농장ID.
- **스텁(미구현, 융합 금지)**: DHI 커넥터, weather(추정값), quarantine 커넥터.

## 다음 작업 (브리핑 후, 우선순위)

1. **경기 잔여 차이 해소** (시연에서 61 미달 시): 좌표 fallback 중인 농장명 목록 확보
   → `PROVINCE_CITIES`에 누락 시·군 추가, 또는 sync 하드코딩 좌표를 실제 좌표로 교체.
   - 근본책: smaXtec 농장 regionId를 시·군 기반 올바른 region으로 백필(DB 접근 필요).
2. **공공데이터 융합 2단계**: EKAPE 실연결 스모크 테스트(도축 개체 1건 등급 조회),
   경락가 캐싱(시세 질문 응답 속도), DHI 커넥터 실연결(젖소 번식 융합 완성).
3. **인수위 슬라이드**: `docs/gyeonggi-deck/` — 경기 카운트 확정치로 수치 최종 검수.

## 환경 메모
- 이 원격 컨테이너는 **데모 DB(클라우드)에 직결 불가**. 모든 검증은 단위 테스트 +
  배포 프리뷰로. postgres MCP/psql 모두 인증 미설정.
- 모델 식별자(claude-opus-4-8)는 커밋/PR/코드에 절대 노출 금지(채팅 한정).
- git: squash 머지 후 로컬 브랜치 stale → `git checkout -B <branch> origin/main` +
  `--force-with-lease`로 푸시.
