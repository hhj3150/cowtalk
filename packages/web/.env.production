# 팅커벨 SSE는 Netlify Edge가 timeout HTML을 캐시하는 문제로
# Railway 직접 호출 (TinkerbellAssistant.tsx가 import.meta.env.VITE_API_BASE_URL 사용).
# 일반 axios 호출(/api/...)은 그대로 Netlify proxy 경유 (apiClient는 baseURL='/api').
VITE_API_BASE_URL=https://cowtalk-server-production.up.railway.app
